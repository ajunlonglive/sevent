#include<iostream>
#include<thread>
#include<string>
#include<atomic>

using namespace std;

class A {
public:
    A() = default;
    A(const A &) = delete;
};

int main(){
    // ATOMIC_FLAG_INIT
    

    return 0;
}